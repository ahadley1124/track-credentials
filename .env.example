# SurrealDB Configuration
# The cloud endpoint URL (without protocol prefix)
SURREAL_URL=your-project.aws-use2.surreal.cloud

# Database credentials
SURREAL_USER=your_username
SURREAL_PASS=your_password

# Note: The application will create a namespace called "track_credentials" 
# and a database called "main" with a "user" table automatically.
